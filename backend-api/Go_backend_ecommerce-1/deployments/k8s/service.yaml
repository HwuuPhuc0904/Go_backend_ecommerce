apiVersion: v1
kind: Service
metadata:
  name: go-backend-service
  labels:
    app: go-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: go-backend